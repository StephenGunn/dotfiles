# Monitor configuration - sourced from host-specific config
# See hosts/ directory for machine-specific settings
# hosts/current.conf is symlinked by link.sh based on hostname

source = ~/.config/hypr/hosts/current.conf
