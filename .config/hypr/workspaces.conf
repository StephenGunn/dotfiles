# Workspace configuration - sourced from host-specific config
# See hosts/ directory for machine-specific settings
# Workspaces are defined in the same host config as monitors

# (workspaces are in hosts/current.conf along with monitors)
