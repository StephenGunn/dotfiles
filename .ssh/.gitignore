# Ignore all SSH private and public keys
id_*
*.pem
*.key

# Ignore authorized keys
authorized_keys

# Ignore known hosts (optional - some people track this)
# known_hosts

# Only track the config file
!config
